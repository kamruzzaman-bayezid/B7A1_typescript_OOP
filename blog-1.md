# কেন TypeScript-এ `any`-এর বদলে `unknown` ব্যবহার করা নিরাপদ?

টাইপস্ক্রিপ্টে আমরা যখন নতুন কাজ শুরু করি, তখন মাঝেমধ্যেই এমন সিচুয়েশনে পড়তে হয় যে একটা ভ্যারিয়েবলের ডেটা আসলে কী আসবে সেটা আমরা শিওর হয়ে বলতে পারি না। তখন আমাদের মাথায় প্রথম বুদ্ধি আসে `any` দিয়ে দিই। কিন্তু এই `any` দেওয়া মানেই হলো নিজের পায়ে নিজে কুড়াল মারা। `any` দিয়ে কাজ করলে টাইপস্ক্রপ্ট এর আসল মজাটাই থাকলো না। এটা টাইপস্ক্রিপ্ট দিয়ে কাজ না করার সমান।

### কেন any কে এড়িয়ে চলবেন?
`any` টাইপ ব্যবহার করা মানে হলো আপনি টাইপস্ক্রিপ্টকে বলছেন, "তোমার এই ভ্যারিয়েবল চেক করার দরকার নাই, আমি যা খুশি করি।" এতে টাইপস্ক্রিপ্ট তার ক্ষমতা হারায়। ফলে এমন হতে পারে যে আপনি একটা নাম্বারের ওপর স্ট্রিংয়ের মেথড চালিয়ে দিলেন, আর কোড রান টাইমে গিয়ে ক্রাশ খেল। এটাকে বলে "type safety hole"।

### তাহলে সমাধান কী?
সমাধান হলো **`unknown`**। এটি `any`-এর মতোই যেকোনো ডেটা নিতে পারে, কিন্তু এটি আপনাকে `any` এর মতো সেই ডেটা সরাসরি ব্যবহার করতে দিবে না। ডেটা ব্যবহার করার আগে এটি আপনাকে বাধ্য করবে আগে চেক করতে যে ডেটাটি আসলে কী টাইপের ডেটা। যাকে আমরা বলি **Type Narrowing**।

**সহজ একটা উদাহরণ দেখি:**
```typescript
let myData: unknown;
myData = "Programming Hero";

// সরাসরি uppercase করতে চাইলে করতে দিবে না। এরর দিবে
// console.log(myData.toUpperCase()); 

// তাই আগে type guard ইউজ করে নিশ্চয়তা দিতে হবে এটা এই টাইপের
if (typeof myData === "string") {
  console.log(myData.toUpperCase()); // এখন আর কোনো এরর নেই!
}